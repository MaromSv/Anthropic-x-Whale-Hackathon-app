<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Required on Android 12+ to load the vendor OpenCL driver from
             /vendor/lib64/libOpenCL.so. Without these, dlopen("libOpenCL.so")
             fails and the LiteRT-LM GPU backend reports "cannot find OpenCL
             library on this device". Manifest merging propagates these into
             any app that depends on :chat-core. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
    </application>
</manifest>

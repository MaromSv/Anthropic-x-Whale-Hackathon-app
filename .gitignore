# Android / Gradle
**/build/
**/.gradle/
android/local.properties
android/app/release/
*.keystore
!android/app/debug.keystore

# Models (too large for git, push via adb)
*.litertlm
*.task
*.bin

# Offline map bundle — fetched/built by scripts/setup_offline_data.sh.
# These blobs total ~470 MB and live in git via build-time download instead.
app/libs/brouter.jar
app/src/main/assets/brouter/segments/*.rd5
app/src/main/assets/tiles/*.mbtiles

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Node / Expo legacy (keep ignoring)
node_modules/
.expo/
